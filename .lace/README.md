# .lace Configuration

- `policies.yaml` — primary governance source for Lace. Duplicate or extend as needed.
- Keep files lightweight; under ~50 KB ensures sub-10 ms load times.
- Commit policies into source control so all contributors share the same invariants.
